
/**
 * Shorthand for `Record<string, any>`.
 */
export type AnyObject = Record<string, any>; // eslint-disable-line @typescript-eslint/no-explicit-any

/**
 * Explicit type for `any` (silencing the linter).
 */
export type AnyType = any; // eslint-disable-line @typescript-eslint/no-explicit-any
